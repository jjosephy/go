package graph


/*
type DirectedDFSstruct {
    g *DirectedGraph
    marked []bool
}
*/


/*
func (g *DirectedGraph) DFS() {
    marked := make([]bool, g.Vertices, g.Vertices)
    g.dfs_int(marked, 0)
}

func (g *DirectedGraph) dfs_int(marked []bool, x int) {
    n, err := g.Adjacent(x)

    if err != nil {
        fmt.Print("invalid node")
        return
    }

    fmt.Printf("%v\n", n)
}
*/
